% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quartoRenderManifest.R
\name{quartoRenderManifest}
\alias{quartoRenderManifest}
\title{Render a selection of project artifacts}
\usage{
quartoRenderManifest(
  manifest,
  root = getwd(),
  only = character(),
  except = character(),
  dryRun = FALSE
)
}
\arguments{
\item{manifest}{Path to a schema 1 or 2 artifact manifest, relative to \code{root}
or absolute.}

\item{root}{Project directory. Defaults to the current working directory.}

\item{only}{Character vector of aliases to include; empty selects all.}

\item{except}{Character vector of aliases to exclude.}

\item{dryRun}{Validate selection, output claims and Quarto source paths
without rendering or requiring existing output files.}
}
\value{
Invisibly, the selected artifact records in manifest order.
}
\description{
Validates the complete manifest's output claims before rendering selected
Quarto masters in manifest order. External products (\code{static} and legacy
\code{map} entries) are checked as existing outputs; their producers never run.
}
\details{
Outputs are checked after the batch: required missing outputs fail,
optional missing outputs are reported. Execution stops on the first render
failure, retaining earlier successful outputs. This is not a transaction.
}
\seealso{
\code{\link[=quartoRender]{quartoRender()}}
}
