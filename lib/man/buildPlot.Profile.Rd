% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildPlot.Profile.R
\name{buildPlot.Profile}
\alias{buildPlot.Profile}
\title{Build a depth-profile plot}
\usage{
buildPlot.Profile(
  data,
  xLegend = "X",
  yLegend = "Y",
  xLimit = NULL,
  yMax = NULL,
  palette = "Batlow",
  lineType = "spline",
  lineSize = 1.6,
  markers = FALSE,
  markerSize = 3,
  showLegend = TRUE,
  legendTitle = "ID",
  plotHeight = 760,
  theme = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} with one row per plotted point.
Required columns are \code{ID} (series identity), \code{X} (measured quantity) and
\code{Y} (depth, positive downward). Optional columns, constant within each
\code{ID}: \code{style} (a dash style accepted by \code{\link[=buildPlot]{buildPlot()}}), \code{size} (line
width), \code{color} (explicit series colour) and \code{symbol} (marker symbol,
one of \code{circle}, \code{square}, \code{diamond}, \code{triangle}, \code{triangle-down}).
Series are drawn in order of first appearance of \code{ID}, which also fixes
palette assignment when \code{color} is absent.}

\item{xLegend}{Abscissa title.}

\item{yLegend}{Ordinate title.}

\item{xLimit}{Positive half-range of the symmetric abscissa: the axis spans
\code{-xLimit} to \code{xLimit}. \code{NULL} derives it from \code{data}. A fixed scale is the
normal choice, because autoscaling a profile of good data renders
measurement noise at full width.}

\item{yMax}{Maximum depth of the ordinate. \code{NULL} derives it from \code{data}.}

\item{palette}{Palette name from \code{\link[grDevices:hcl.pals]{grDevices::hcl.pals()}}, used for every
series without an explicit \code{color}.}

\item{lineType}{Series geometry, \code{"spline"} or \code{"line"}.}

\item{lineSize}{Default line width for series without a \code{size} value.}

\item{markers}{Draw a marker at every plotted point.}

\item{markerSize}{Marker radius in pixels.}

\item{showLegend}{Show the series legend. Keep it enabled on every panel of
a composed figure: a legend attached to one panel of a pair shortens that
panel and leaves the other unlabelled.}

\item{legendTitle}{Legend title.}

\item{plotHeight}{Widget height in pixels. Width stays responsive.}

\item{theme}{A Highcharts theme object.}
}
\value{
A \code{highchart} htmlwidget. Inputs are not modified and no file is
written.
}
\description{
Builds one Highcharts widget for a quantity measured along a borehole or
any other downward axis: the ordinate is depth, growing downward, and the
abscissa is the measured quantity on a fixed symmetric scale. One series is
drawn per \code{ID}.

The function renders a single panel. Composing several panels — for example
the two orthogonal axes of an inclinometer side by side — belongs to the
caller, which calls this function once per panel and lays the widgets out.

Compared with \code{\link[=buildPlot]{buildPlot()}} this builder adds explicit per-series colour,
per-series marker symbols, and a legend switch, and it defaults to a fixed
symmetric abscissa. It does not select surveys, order series, or decide the
reference: the caller owns all of that.
}
\examples{
Depth <- seq(0.5, 20, by = 0.5)
Data <- rbind(
  data.frame(ID = "baseline", X = 0, Y = Depth, style = "longdashdotdot",
    size = 1),
  data.frame(ID = "2026-04-01", X = 12 * (1 - Depth / 20)^2, Y = Depth,
    style = "solid", size = 1.6)
)
buildPlot.Profile(
  data = Data,
  xLegend = "Displacement (mm)",
  yLegend = "Depth (m)",
  xLimit = 25
)

}
