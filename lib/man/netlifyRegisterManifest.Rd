% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netlifyManifest.R
\name{netlifyRegisterManifest}
\alias{netlifyRegisterManifest}
\alias{netlifyDeployManifest}
\alias{netlifyDomainManifest}
\title{Register, publish or configure the Netlify sites of a manifest selection}
\usage{
netlifyRegisterManifest(
  manifest,
  root = getwd(),
  only = character(),
  except = character(),
  create = FALSE,
  account = NULL,
  dryRun = FALSE
)

netlifyDeployManifest(
  manifest,
  root = getwd(),
  only = character(),
  except = character(),
  prod = FALSE,
  dryRun = FALSE
)

netlifyDomainManifest(
  manifest,
  root = getwd(),
  only = character(),
  except = character(),
  https = FALSE,
  dryRun = FALSE
)
}
\arguments{
\item{manifest}{Path to a schema 1 or 2 artifact manifest, relative to \code{root}
or absolute.}

\item{root}{Project directory. Defaults to the current working directory.}

\item{only}{Character vector of aliases to include; empty selects all.}

\item{except}{Character vector of aliases to exclude.}

\item{create}{Permit creating missing sites. Defaults to \code{FALSE}.}

\item{account}{Optional Netlify account slug.}

\item{dryRun}{Describe the request without provider calls or registry writes.}

\item{prod}{Publish to production instead of creating a draft.}

\item{https}{Enable HTTPS, including certificate provisioning if needed.}
}
\value{
Invisibly, the value of the direct operation for the artifacts it
reached; an empty character vector when every selected upload was skipped.
}
\description{
Manifest forms of \code{\link[=netlifyRegister]{netlifyRegister()}}, \code{\link[=netlifyDeploy]{netlifyDeploy()}} and
\code{\link[=netlifyDomain]{netlifyDomain()}}. Each reads the artifact manifest, applies the alias
selection and validates the whole selection before its first effect.
Registration and domains take each artifact's \code{siteSlug} and \code{domain}; those
values must be unique across the complete manifest, not only the selection,
and every selected artifact must declare the one in use. Publication uploads
each selected artifact's \code{path}: a missing \code{index.html} fails a required
artifact and skips an optional one with a message.
}
\details{
Batches are sequential, not transactions; a later failure retains
earlier effects. Dry runs contact no provider and write no registry.
}
\seealso{
\code{\link[=quartoRenderManifest]{quartoRenderManifest()}}
}
