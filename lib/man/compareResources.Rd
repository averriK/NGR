% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{compareResources}
\alias{compareResources}
\title{Compare project resources with their sources}
\usage{
compareResources(
  from = NULL,
  source = NULL,
  paths = character(),
  root = getwd()
)
}
\arguments{
\item{from}{Character vector of source manifest paths to compare against
instead of the recorded locations, or \code{NULL} to use existing project
associations. Associations are read, never re-pointed. Incompatible with a
nonempty \code{source}.}

\item{source}{Registered source identities, or \code{NULL} for all associations.}

\item{paths}{Destination files or directory prefixes. Empty selection uses
the enrolled selection, or the complete source when \code{from} is supplied.}

\item{root}{Existing project directory; defaults to the working directory.}
}
\value{
A list with \code{files} (source, state, path and modified columns) and
\code{changed}, which is true for differences, missing/retired files or edits
against a receipt. Customized seeds alone do not set \code{changed}.
}
\description{
Reads sources, local files and per-file receipts without writing them.
}
