% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resources.R
\name{pullResources}
\alias{pullResources}
\title{Incorporate and update project resources}
\usage{
pullResources(
  from = NULL,
  source = NULL,
  paths = character(),
  root = getwd(),
  force = FALSE,
  dryRun = FALSE
)
}
\arguments{
\item{from}{Character vector of source manifest paths, or \code{NULL} to use
existing project associations. Relative paths resolve from the R working
directory. A source is identified by its \code{id}: a manifest whose \code{id} is
already associated re-points that association to the new location, with a
message. Incompatible with a nonempty \code{source}.}

\item{source}{Registered source identities, or \code{NULL} for all associations.}

\item{paths}{Destination files or directory prefixes. Empty selection uses
the enrolled selection, or the complete source when \code{from} is supplied.}

\item{root}{Existing project directory; defaults to the working directory.}

\item{force}{Replace different managed files; never replace existing seeds
or override incompatible source claims.}

\item{dryRun}{Return the same validated plan without writing files.}
}
\value{
A list containing \code{actions} (a data frame with source, path and
action columns) and \code{manifest} (the resulting project manifest).
}
\description{
Sources declare mappings to shared project paths. All implicated claims are
checked before writes, including claims of sources not selected for update.
Existing seeds, extra project files and scientific data are preserved. Files
retired from a source are not deleted. Ordinary write errors restore the
files already written; concurrent writers and process termination are not
covered by this recovery.
}
