% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildSpectrumPlot.R
\name{buildSpectrumPlot}
\alias{buildSpectrumPlot}
\title{Build an acceleration response spectrum plot}
\usage{
buildSpectrumPlot(data, logScale, fill, fillSize)
}
\arguments{
\item{data}{Prepared line data with the \code{ID}, \code{X}, \code{Y}, and optional
\code{style} and \code{size} columns accepted by \code{\link[=buildPlot]{buildPlot()}}. \code{X} is period in
seconds; \code{Y} is acceleration in g. The input is not modified.}

\item{logScale}{Whether the acceleration axis is logarithmic. When true,
zero-period points are displayed with the PGA label using the zero-period
convention of \code{\link[=buildPlot]{buildPlot()}}.}

\item{fill}{Whether to draw the minimum--maximum band across supplied
series. The caller decides which series may be enveloped together.}

\item{fillSize}{Width of the dotted minimum and maximum boundary lines.}
}
\value{
A \code{highchart} htmlwidget, with the same empty-input and validation
behavior as \code{\link[=buildPlot]{buildPlot()}}. No file is written and no project data or
session variables are read.
}
\description{
Draws prepared acceleration spectra using \code{\link[=buildPlot]{buildPlot()}}. Periods are in
seconds and spectral accelerations in g. The caller owns selection,
calculation, series names, order, line styles and widths. This function
applies the shared spectrum presentation: spline lines, logarithmic period
axis, a horizontal legend, gridlines and an optional minimum--maximum band.
}
