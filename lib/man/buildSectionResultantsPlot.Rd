% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildSectionResultantsPlot.R
\name{buildSectionResultantsPlot}
\alias{buildSectionResultantsPlot}
\title{Build circular-section resultant diagrams}
\usage{
buildSectionResultantsPlot(
  curves,
  rays,
  referenceRadius,
  panelTitles = c(N = "N", M = "M", Q = "Q"),
  positionLabels = c(top = "Top", right = "Right", bottom = "Bottom", left = "Left"),
  subtitle = NULL,
  plotHeight = 560
)
}
\arguments{
\item{curves}{A data frame for one or two cases, with one ordered,
explicitly closed curve for every \code{case} and \code{resultant}. Required
columns are \code{case}, \code{prescription}, \code{resultant}, \code{thetaDeg}, \code{value},
\code{unit}, \code{x}, \code{y}, and \code{radialFraction}. Character columns must be
non-empty; each case must have one \code{prescription}, and each resultant one
\code{unit}. Numeric columns must be finite. \code{resultant} must contain one or
more of \code{N}, \code{M}, and \code{Q}. The first and last \code{x}, \code{y} coordinates of
every curve must coincide. \code{radialFraction} is a non-negative,
dimensionless upper
bound on the absolute display-radius offset divided by
\code{referenceRadius}; it is used only to set panel limits.}

\item{rays}{A data frame with one row per independent radial ordinate.
Required columns are \code{case}, \code{resultant}, \code{sign}, \code{xSection}, \code{ySection},
\code{x}, and \code{y}. Character columns must be non-empty and numeric columns
finite. Every case-resultant combination in \code{curves} must occur in
\code{rays}, and every ordinate must start on the reference circle.
\code{sign} must be \code{"positive"} or \code{"negative"}; the caller owns that
classification and the angular sampling.}

\item{referenceRadius}{Positive radius of the reference circle, in the same
display-coordinate units as \code{x} and \code{y}.}

\item{panelTitles}{Character vector whose names include every resultant
present in \code{curves}. Values are the visible panel titles and should
include physical units where applicable.}

\item{positionLabels}{Character vector named \code{top}, \code{right}, \code{bottom}, and
\code{left}. Values label the four cardinal positions of the section.}

\item{subtitle}{Optional character scalar displayed above the panels.}

\item{plotHeight}{Positive widget height of at least 360 pixels. Width
remains responsive, following the sizing contract used by \code{\link[=buildPlot]{buildPlot()}}.}
}
\value{
A \code{highchart} htmlwidget. Inputs are not modified and no file is
written.
}
\description{
Builds one responsive Highcharts widget with square Cartesian panels for
any non-empty subset of the \code{N}, \code{M}, and \code{Q} resultants of a circular
section. The caller supplies
display coordinates for the closed curves and for the independent radial
ordinates. The function only renders those layers: it does not calculate
resultants, select ordinate angles, classify signs, or transform physical
values into display radii.
}
\examples{
Theta <- seq(0, 2 * pi, length.out = 9)
Amplitudes <- c(N = 0.20, M = 0.15, Q = 0.10)
Curves <- do.call(rbind, lapply(names(Amplitudes), function(Resultant) {
  Amplitude <- Amplitudes[[Resultant]]
  Radius <- 1 + Amplitude * cos(2 * Theta)
  data.frame(
    case = "example",
    prescription = "Example",
    resultant = Resultant,
    thetaDeg = Theta * 180 / pi,
    value = cos(2 * Theta),
    unit = "unit",
    x = Radius * sin(Theta),
    y = Radius * cos(Theta),
    radialFraction = abs(Amplitude)
  )
}))
Rays <- Curves[Curves$thetaDeg < 360, c("case", "resultant", "x", "y")]
Rays$xSection <- sin(Curves$thetaDeg[Curves$thetaDeg < 360] * pi / 180)
Rays$ySection <- cos(Curves$thetaDeg[Curves$thetaDeg < 360] * pi / 180)
Rays$sign <- ifelse(Curves$value[Curves$thetaDeg < 360] >= 0, "positive", "negative")
buildSectionResultantsPlot(
  curves = Curves,
  rays = Rays,
  referenceRadius = 1,
  panelTitles = c(N = "N [unit]", M = "M [unit]", Q = "Q [unit]"),
  positionLabels = c(top = "Top", right = "Right", bottom = "Bottom", left = "Left")
)

}
