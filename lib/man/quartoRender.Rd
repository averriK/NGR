% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quartoRender.R
\name{quartoRender}
\alias{quartoRender}
\title{Render a Quarto document in a disposable project copy}
\usage{
quartoRender(
  input,
  profile,
  root = getwd(),
  output = NULL,
  args = character(),
  manifest = "manifest.json"
)
}
\arguments{
\item{input}{Project-relative QMD or Markdown master.}

\item{profile}{One of \code{book}, \code{html}, \code{revealjs} or \code{docx}.}

\item{root}{Project directory. Defaults to the current working directory.}

\item{output}{Output directory relative to \code{root}. HTML formats require
\verb{html/<name>}; DOCX requires \code{docx}. When \code{NULL}, uses \verb{html/<master stem>}
or \code{docx}, respectively.}

\item{args}{Character vector of additional arguments passed to Quarto.}

\item{manifest}{Path to the project provenance manifest, relative to \code{root}
or absolute. An absent manifest produces a draft publication stamp.}
}
\value{
Invisibly, a character vector of delivered file paths.
}
\description{
Uses \verb{yml/_quarto.yml} and \verb{yml/_quarto-<profile>.yml} from the project.
Book masters supply chapters and appendices through their frontmatter.
Single-file masters are moved to the root of the disposable copy so their
resource paths resolve from the project root. Source files are not changed.
}
\details{
Requires Quarto on \code{PATH}; DOCX also requires Python 3.9 or later
with \code{lxml} in that interpreter (\code{python3}, or \code{python} on Windows).
DOCX uses the master's \code{title} and \code{lang} (English when absent), and reads
\code{params.client.name}, \code{params.consultant.name} and \code{params.project_id} from
the project's \code{params.yml}. Required values are non-empty single-line
strings; missing values fail before Quarto. The date is calculated for the
render in \code{dd/mm/yyyy}, matching the print stamp. Cover labels follow
English or Spanish. A master \code{srk} block is not read. Other Word metadata
is left blank for editing in Word. The CAN inner title page uses optional
\code{address} lists and \code{web} strings from the client and consultant blocks,
together with the output filename. The project DOCX profile must use a
CAN-compatible reference and \code{number-sections: true}. Each appendix file
declared in \code{appendices} needs one numbered level-1 heading with an explicit
identifier (\verb{\{#sec-...\}}), which selects its separator. A book without
\code{appendices} emits a diagnostic and receives no CAN appendix separators.
Input DOCX sections within the body are currently unsupported.
Preliminaries before the first Heading1 use Roman numbering; the body
starts at page 1. The profile enables a Word table of contents, which may
need a manual update. No field-update request is set on document opening.
The installed library owns the Python compositor and semantic filters.
Quarto resolves citations and cross-references once for the complete book.
Rendering and composition finish before publication begins. HTML output
replaces the selected directory; each DOCX replaces its destination through
a sibling temporary file, keeping other documents. Publication of multiple
files is not a transaction. Temporary files, the working directory and the
publication-stamp environment variable are restored on ordinary exit.
}
