% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knitBlock.R
\name{knitBlock}
\alias{knitBlock}
\title{Knit a QMD block with dynamic labels}
\usage{
knitBlock(
  path,
  stem,
  vars = list(),
  labels = c("parent", "rewrite", "strip", "keep"),
  root = getwd()
)
}
\arguments{
\item{path}{Character scalar. QMD fragment path. Absolute paths are used as
supplied; paths starting with \code{/} are resolved below \code{root}.}

\item{stem}{Character scalar or vector used to prefix labels when
\code{labels = "rewrite"}.}

\item{vars}{Named list of variables visible to the child document.}

\item{labels}{One of \code{"parent"}, \code{"rewrite"}, \code{"strip"}, or \code{"keep"}.
\code{"parent"} rewrites labels in \code{path} and strips labels/captions from
included child blocks.}

\item{root}{Project root used to resolve absolute-looking include paths.}
}
\value{
Invisibly returns the knitted child markdown. The markdown is also
emitted with \code{cat()} for use in \code{results: asis} chunks.
}
\description{
Reads a QMD fragment, expands Quarto include shortcodes recursively, rewrites
static figure/table labels with a caller-provided stem, and emits the result
through \code{knitr::knit_child()}. The default \code{labels = "parent"} rewrites
labels in the requested block and strips labels/captions from recursively
included child blocks.
}
