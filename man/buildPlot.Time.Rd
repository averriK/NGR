% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildPlot.Time.R
\name{buildPlot.Time}
\alias{buildPlot.Time}
\title{Build a time-series plot}
\usage{
buildPlot.Time(
  data,
  xLegend = "Date",
  yLegend = "Y",
  yLimits = NULL,
  referenceLines = NULL,
  y2Legend = NULL,
  y2Offset = 0,
  y2Scale = 1,
  y2Decimals = 1,
  palette = "Dark 3",
  lineSize = 2,
  markers = TRUE,
  markerSize = 3,
  rangeSelector = TRUE,
  navigator = TRUE,
  dateFormat = "\%d/\%m/\%Y",
  showLegend = TRUE,
  legendTitle = "ID",
  plotHeight = 430,
  theme = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} with one row per plotted point.
Required columns are \code{ID} (series identity), \code{X} (\code{Date} or \code{POSIXct})
and \code{Y} (measured value, \code{NA} where the record has a gap). Optional
columns, constant within each \code{ID}: \code{type} (\code{"line"} or \code{"scatter"}),
\code{style} (dash style), \code{size} (line width), \code{color} (explicit colour) and
\code{symbol} (marker symbol). Series are drawn in order of first appearance
of \code{ID}.}

\item{xLegend}{Abscissa title.}

\item{yLegend}{Ordinate title.}

\item{yLimits}{Length-two increasing numeric vector fixing the ordinate, or
\code{NULL} to autoscale.}

\item{referenceLines}{A data frame of labelled horizontal reference lines,
or \code{NULL}. Required columns are \code{value} and \code{label}; optional \code{color},
\code{width} and \code{style}. Drawn above the series and outside the legend.}

\item{y2Legend}{Title of a linked right axis, or \code{NULL} for none. The right
axis relabels the ordinate as \code{y2Offset + y2Scale * value}; it is a
restatement of the same measurement, valid only while that relation
holds.}

\item{y2Offset}{Additive term of the right-axis relation.}

\item{y2Scale}{Multiplicative term of the right-axis relation.}

\item{y2Decimals}{Decimals shown on the right axis.}

\item{palette}{Palette name from \code{\link[grDevices:hcl.pals]{grDevices::hcl.pals()}}, used for every
series without an explicit \code{color}.}

\item{lineSize}{Default line width for series without a \code{size} value.}

\item{markers}{Draw a marker at every plotted point.}

\item{markerSize}{Marker radius in pixels.}

\item{rangeSelector}{Show the range selector.}

\item{navigator}{Show the navigator.}

\item{dateFormat}{Highcharts date format used in the tooltip header.}

\item{showLegend}{Show the series legend.}

\item{legendTitle}{Legend title.}

\item{plotHeight}{Widget height in pixels. Width stays responsive.}

\item{theme}{A Highcharts theme object.}
}
\value{
A \code{highchart} htmlwidget of Highcharts Stock type. Inputs are not
modified and no file is written.
}
\description{
Builds one Highcharts Stock widget for one or more series measured against
calendar time. The abscissa is a real datetime axis, so the reader sees
dates rather than a numeric surrogate, and the widget carries a range
selector and a navigator for a record that spans years.

Missing values break the line instead of being bridged, which keeps a gap
in the record visually distinct from a measured value. Labelled reference
lines can be drawn on the ordinate for design levels, thresholds, or
physical bounds, and an optional linked right axis restates the ordinate
under an affine transform.
}
\examples{
Day <- as.Date("2020-01-01") + seq(0, 720, by = 30)
Data <- data.frame(
  ID = "level",
  X = Day,
  Y = 100 + sin(seq_along(Day) / 2)
)
buildPlot.Time(
  data = Data,
  xLegend = "Date",
  yLegend = "Level (m)",
  referenceLines = data.frame(value = 101.5, label = "threshold")
)

}
