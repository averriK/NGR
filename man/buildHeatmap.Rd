% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildHeatmap.R
\name{buildHeatmap}
\alias{buildHeatmap}
\title{Build a heatmap plot (Highcharts)}
\usage{
buildHeatmap(
  .data,
  xAxis.legend = "X",
  yAxis.legend = "Y",
  plot.title = NULL,
  colorAxis.min = 0,
  colorAxis.max = NULL,
  color.stops = NULL,
  series.name = "Value",
  border.width = 1,
  border.color = "#333333",
  dataLabels.show = FALSE,
  tooltip.format = NULL,
  legend.align = "right",
  legend.layout = "vertical",
  legend.valign = "middle",
  plot.theme = NULL
)
}
\arguments{
\item{.data}{A data.table with columns X, Y (raw category values) and Z (numeric).}

\item{xAxis.legend}{Character; x-axis title. Default \code{"X"}.}

\item{yAxis.legend}{Character; y-axis title. Default \code{"Y"}.}

\item{plot.title}{Character or NULL; chart title. Default NULL (no title).}

\item{colorAxis.min}{Numeric; lower bound of the color axis. Default \code{0}.}

\item{colorAxis.max}{Numeric or NULL; upper bound of the color axis.
NULL (default) uses \code{ceiling(max(.data$Z))}.}

\item{color.stops}{List of Highcharts color-axis stops, each a \code{list(position, color)}.
NULL (default) uses a 10-stop Inferno palette.}

\item{series.name}{Character; legend label for the series. Default \code{"Value"}.}

\item{border.width}{Numeric; cell border width in pixels. Default \code{1}.}

\item{border.color}{Character; cell border color. Default \code{"#333333"}.}

\item{dataLabels.show}{Logical; show value labels inside cells. Default \code{FALSE}.}

\item{tooltip.format}{Character; Highcharts \code{pointFormat} string. NULL (default)
generates a format using \code{xAxis.legend}, \code{yAxis.legend}, \code{series.name},
\code{{point.xLabel}}, and \code{{point.yLabel}}. For custom tooltips, use
\code{{point.xLabel}} and \code{{point.yLabel}} for the original \code{.data$X} and
\code{.data$Y} values; \code{{point.x}} and \code{{point.y}} are internal 0-based indices.}

\item{legend.align}{Character; horizontal legend alignment. Default \code{"right"}.}

\item{legend.layout}{Character; legend layout direction. Default \code{"vertical"}.}

\item{legend.valign}{Character; vertical legend alignment. Default \code{"middle"}.}

\item{plot.theme}{Optional Highcharts theme object (e.g. \code{hc_theme_538_gridlines()}).
Default NULL applies no theme.}
}
\value{
A highchart object.
}
\description{
Build a Highcharts heatmap from a data.table with columns X, Y, Z.

X and Y are treated as categorical axes: unique values are sorted and used as
category labels. Z is the numeric value mapped to the color axis. The function
converts the raw X/Y values to 0-based integer indices internally, and stores
the original labels in each point as \code{xLabel} and \code{yLabel}.

Color axis defaults to the Inferno palette (10 stops). Supply \code{color.stops}
to override. \code{colorAxis.max} defaults to \code{ceiling(max(Z))} when NULL.
}
