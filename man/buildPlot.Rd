% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildPlot.R
\name{buildPlot}
\alias{buildPlot}
\title{Build a highchart plot}
\usage{
buildPlot(
  library = NULL,
  plot.type = NULL,
  data.lines = NULL,
  data.points = NULL,
  line.type = "line",
  plot.title = NULL,
  plot.subtitle = NULL,
  plot.height = NULL,
  plot.width = NULL,
  xAxis.legend = "X",
  yAxis.legend = "Y",
  group.legend = "ID",
  color.palette = "Dark 3",
  line.style = "solid",
  point.style = "circle",
  line.size = 1,
  point.size = 3,
  xAxis.log = FALSE,
  yAxis.log = FALSE,
  xAxis.reverse = FALSE,
  yAxis.reverse = FALSE,
  xAxis.max = NA,
  yAxis.max = NA,
  xAxis.min = NA,
  yAxis.min = NA,
  xAxis.label = TRUE,
  yAxis.label = TRUE,
  legend.layout = "horizontal",
  legend.align = "right",
  legend.valign = "top",
  legend.show = TRUE,
  plot.save = FALSE,
  plot.theme = NULL,
  xAxis.legend.fontsize = "14px",
  yAxis.legend.fontsize = "14px",
  group.legend.fontsize = "12px",
  plot.title.fontsize = "24px",
  plot.subtitle.fontsize = "18px",
  print.max.abs = FALSE,
  point.dataLabels = FALSE,
  plot.filename = NULL,
  fill.opacity = 0.3,
  fill.legend = NULL,
  fill.max = ".max",
  fill.min = ".min",
  fill.minmax = FALSE,
  fill.max.style = "Solid",
  fill.min.style = "Solid",
  fill.max.size = NULL,
  fill.min.size = NULL,
  fill.max.color = "#00008B",
  fill.min.color = "#8B0000",
  xAxis.log.offset = NULL,
  yAxis.log.offset = NULL,
  interpolation.method = "linear",
  yAxis2 = NULL,
  yAxis2.legend = NULL,
  yAxis2.transform = NULL,
  yAxis2.decimals = 0
)
}
\arguments{
\item{library}{DEPRECATED. A placeholder parameter that triggers a warning if used.}

\item{plot.type}{DEPRECATED. A placeholder parameter that triggers a warning if used.}

\item{data.lines}{A data.table containing columns "ID", "X", "Y", optional "style", optional "size", optional "fill", optional "yAxis" (0 or 1).
If NULL, no lines will be plotted.}

\item{data.points}{A data.table containing columns "ID", "X", "Y", optional "style", optional "yAxis" (0 or 1).
If NULL, no scatter points will be plotted.}

\item{line.type}{A string indicating the line series type. Options might include \code{"line"} or \code{"spline"}.
If invalid, \code{"line"} is used.}

\item{plot.title}{A string for the plot title}

\item{plot.subtitle}{A string for the plot subtitle}

\item{plot.height}{A numeric for the plot height}

\item{plot.width}{A numeric for the plot width}

\item{xAxis.legend}{A string for the x-axis legend}

\item{yAxis.legend}{A string for the y-axis legend}

\item{group.legend}{A string for the legend title}

\item{color.palette}{A string for the color palette (must exist in \code{grDevices::hcl.pals()})}

\item{line.style}{A string specifying the default line style if \code{data.lines$style} is missing/invalid.}

\item{point.style}{A string specifying the default point marker style if \code{data.points$style} is missing/invalid.}

\item{line.size}{A numeric for the line width}

\item{point.size}{A numeric for the point size}

\item{xAxis.log}{A logical for the x-axis log scale}

\item{yAxis.log}{A logical for the y-axis log scale}

\item{xAxis.reverse}{A logical for the x-axis reverse}

\item{yAxis.reverse}{A logical for the y-axis reverse}

\item{xAxis.max}{A numeric for the x-axis max}

\item{yAxis.max}{A numeric for the y-axis max}

\item{xAxis.min}{A numeric for the x-axis min}

\item{yAxis.min}{A numeric for the y-axis min}

\item{xAxis.label}{A logical for the x-axis label}

\item{yAxis.label}{A logical for the y-axis label}

\item{legend.layout}{A string for the legend layout}

\item{legend.align}{A string for the legend horizontal alignment (e.g. \code{"center"}, \code{"left"}, \code{"right"})}

\item{legend.valign}{A string for the legend vertical alignment (e.g. \code{"top"}, \code{"middle"}, \code{"bottom"})}

\item{legend.show}{A logical for the legend show/hide}

\item{plot.save}{A logical for the plot save}

\item{plot.theme}{A highchart theme object}

\item{xAxis.legend.fontsize}{A string for the x-axis legend fontsize}

\item{yAxis.legend.fontsize}{A string for the y-axis legend fontsize}

\item{group.legend.fontsize}{A string for the legend items' fontsize}

\item{plot.title.fontsize}{A string for the plot title fontsize}

\item{plot.subtitle.fontsize}{A string for the plot subtitle fontsize}

\item{print.max.abs}{A logical for printing max absolute Y labels as annotations (only for lines)}

\item{point.dataLabels}{A logical for whether data labels appear for points}

\item{plot.filename}{A string for the plot filename, if saving}

\item{fill.opacity}{Opacity for arearange fill (0-1).}

\item{fill.legend}{Optional legend label for the shaded arearange band. If NULL or empty, a default label is used.}

\item{fill.max}{ID to use for the upper envelope series when auto-envelopes are enabled.}

\item{fill.min}{ID to use for the lower envelope series when auto-envelopes are enabled.}

\item{fill.minmax}{Logical flag; if TRUE and no explicit fill == TRUE is present in data.lines, automatically
constructs upper/lower envelope series by X (using fill.max / fill.min as IDs) and shades between them.}

\item{fill.max.style}{Line style for upper envelope (default: "Solid").}

\item{fill.min.style}{Line style for lower envelope (default: "Solid").}

\item{fill.max.size}{Line width for upper envelope (default: NULL, uses global line.size).}

\item{fill.min.size}{Line width for lower envelope (default: NULL, uses global line.size).}

\item{fill.max.color}{Color for upper envelope (default: "#00008B", dark blue).}

\item{fill.min.color}{Color for lower envelope (default: "#8B0000", dark red).}

\item{xAxis.log.offset}{Offset value for X when xAxis.log=TRUE and data contains X<=0. NULL (default) calculates automatically as min of positive X values times 0.1. FALSE disables transformation. Numeric value uses that as offset.}

\item{yAxis.log.offset}{Offset value for Y when yAxis.log=TRUE and data contains Y<=0. NULL (default) calculates automatically as min of positive Y values times 0.1. FALSE disables transformation. Numeric value uses that as offset.}

\item{interpolation.method}{A string specifying the interpolation method used by \code{approx()}.}

\item{yAxis2}{Advanced: optional list with Highcharts \code{yAxis[1]} options (secondary axis). If provided, it overrides \verb{yAxis2.*} convenience arguments.}

\item{yAxis2.legend}{Optional string for the secondary Y axis title (right axis).}

\item{yAxis2.transform}{Optional one-sided formula to transform primary Y tick values for secondary axis labels in linked mode. Use \code{Y} as the primary axis tick value (e.g. \code{~ 1 / Y}).}

\item{yAxis2.decimals}{Integer number of decimals for secondary axis labels when using \code{yAxis2.transform} (passed to \code{Highcharts.numberFormat}).}
}
\value{
A highchart object if either \code{data.lines} or \code{data.points} is provided.
Returns NULL if both are NULL, with a soft warning.
}
\description{
Build a highchart plot using separate data sources for lines and points.

Supports the plotting of line and scatter series. Includes optional arearange shading between
two line groups if indicated in \code{data.lines$fill}, and interpolation is handled via the \code{approx} function
(which can be customized using the \code{interpolation.method} argument). Fallback behaviors:
if an invalid \code{line.type} is provided, \code{"line"} is used; if an invalid or missing line style is found,
\code{"solid"} is used; if an invalid \code{color.palette} is provided, a default Highcharts palette is chosen.
}
