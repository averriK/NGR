# Handoff a la sesión que audita NGR

2026-09-20. De la sesión que hizo la etapa 1 (cadena `cli-repair`, desde el
2026-09-17) a la sesión que tomó este `STATE.md` y está auditando NGR contra
qrt/psha. Escrito por pedido del propietario.

**No toqué tu `STATE.md`.** Lo reescribiste con tu objetivo y está sin
commitear; escribir encima sería repetir un error que ya cometí hoy en otro
repositorio. Esto es un archivo aparte.

## 1. Lo que no vas a encontrar leyendo el código

El propietario me ordenó limpiar `dev/` de planes y fósiles, y borré del árbol
el plan, cinco handoffs, dos avisos, la propuesta de datos y toda la evidencia
congelada de `dev/SoT/cli-repair/` (1,7 MB). Viven en Git:

```bash
git show 02ec950:dev/plan/cli-repair/PLAN.md          # el plan, 152 líneas
git log --diff-filter=D -- dev/SoT/cli-repair          # la evidencia borrada
git show 02ec950:dev/plan/cli-repair/HANDOFF-PRUEBAS-REALES-20260918.md
```

`PLAN.md` está **restaurado en el árbol** (`6a55e69`): tiene los ocho candidatos
R1–R8 con su defecto observado, el oráculo viejo que fallaba y la diferencia
pedida. Es lo que explica por qué cada cambio existe.

Lo que te sirve de ahí, en una línea cada uno: R1 hidratar desde ubicación
arbitraria, R2 masters hidratables, R4 destinos de publicación sembrados,
R5 identidad de revisión sin git, R6 parser en `cli/main.R` llamando sólo
exports, R8 rastros del nombre qrt, R9 convivencia de `qrt.manifest.json` con
`manifest.json`.

## 2. Tu hallazgo de `products/hazard` es real, y el defecto es mío

Lo reproduje independientemente sobre `lib/R/resources.R:279-293`
(`.declaredRoots`), con un centinela en la raíz declarada y un recurso de
scaffold apuntando encima:

| `path.data` declarado | Resultado |
|---|---|
| `tablas` | rechazado; el centinela sobrevive |
| `./tablas` | **escribe encima**; el centinela se pierde |
| ruta absoluta dentro del proyecto | **escribe encima**; el centinela se pierde |

La causa es mía y está en dos líneas de esa función: descarto los valores
absolutos (`grepl("^(/|[A-Za-z]:)", Value)` → `next`) y descarto el segmento
`.` (`Segment %in% c(".", "..")` → `next`). En los dos casos la raíz nunca se
registra, así que `pull --force` la trata como territorio libre y **no avisa
nada**. Es exactamente la clase de falla silenciosa que esa protección
existía para evitar.

Tu corrección del propietario es correcta en lo suyo: `products/hazard` no
existe en ningún proyecto y la sonda no acredita afectación real. Pero el
defecto del código sí es real y sobrevive a la corrección del fixture: hoy
ningún contrato declara `./` ni rutas absolutas, y por eso no hay impacto en
producción. **No lo reparé**: es tu auditoría, y el propietario no me lo
asignó. Si te lo pide a vos, es una función.

## 3. Estado verificable, con sus fechas

- **Etapa 1 publicada** en `origin/main`: R1 `3f27df4`, R2 `a5e9788`,
  R4 `277ec75`, R5 `8a08f43`, R6 `d8c9159`, R8 `acd0d00`, R9 `15d4fbf`,
  L2 `438a375`, kit de familia `5878651`, instalador AOM 0.2.0 `18b6d91`.
- **Aceptación real** contra el oráculo: deck `sha` de AR-S2L1W byte-idéntico
  entre qrt+psha y ngr, salvo el sello de revisión y los ids de widget.
- **El scaffold del informe** (`libraries/reports`, dev/main `6672b0d`) lee los
  datos por contrato del productor, no por ruta fija. Falla nombrando contrato,
  clave y ruta. Cero coincidencias de `oq/data` en todo el scaffold.
- **Los 14 contratos** de los 7 proyectos están publicados y validados con las
  librerías publicadas: `hazard process --dry-run` exit 0 en los 7,
  `newmark process --steps dn,kmax --dry-run` exit 0 en los 6 con productos.
- **Instalación observada hoy 15:12**: build `1e4b68b-dirty`. Es anterior al
  arreglo del `--help` (`12a80c6`), así que el comando instalado **sigue
  emitiendo la ayuda vieja**, la que narra el motor. No es un hallazgo nuevo:
  está reportado en `AOM/dev/REPORTE-NGR-HELP-CONTAMINADO-20260920.md`.

## 4. Trampas medidas, para que no las pagues de nuevo

1. `cli/tests` exige `/usr/local/bin` delante de `/usr/bin` en el PATH —el
   oráculo qrt usa `env bash` con `set -u` y necesita bash ≥ 4.4— y que exista
   `NGR_TEST_ROOT`. Sin eso aparecen fallos espurios que no son de NGR.
2. El literal `ngr` de `--from` se traduce en `cli/main.R:125` antes de mirar
   el sistema de archivos: un manifiesto local llamado exactamente `ngr` queda
   inalcanzable y el usuario cree que hidrató el suyo. Vos encontraste la vuelta
   —`./ngr` sí lo selecciona—; ese matiz no estaba en mi reporte y lo agrego acá.
3. Comprobar LFS por `grep lfs .git/config` da falso negativo: los filtros
   pueden estar en `~/.gitconfig`. Usá `git config --show-origin --get filter.lfs.clean`.
4. Antes de commitear en un repo de proyecto, mirá el índice: hoy hay una
   migración de otro agente con 210 renombrados preparados en AR-M2V4D.

## 5. Errores míos en esta cadena, sin adorno

Los cuatro primeros me los marcó el propietario:

- Afirmé que el instalador funcionaba bajo `sudo` sin haberlo ejecutado.
- Listé `Vs30.gmdp` como una clave que había que ubicar, sin ver que
  `reports/sha/scripts/setup/global.R:23` la deriva sin guarda.
- Cambié la forma de `Dn_weights` llamándola defecto, sin leer cómo parsea el
  contrato el CLI de newmark. Resultó inocuo, pero el razonamiento era inválido.
- Contaminé `ngr --help` con vocabulario del motor de recursos, inventándolo:
  qrt y psha ya tenían la forma correcta.
- Y el que te afecta: borré los papeles de esta cadena mientras vos la tomabas.

## 6. Quién tiene qué pendiente

| Dueño | Pendiente |
|---|---|
| Propietario | Reinstalar para que el `--help` corregido llegue a `/usr/local`; las 20 tablas de AR-M2V4D en git plano frente a su `.gitattributes`; su lectura del 18,8% de filas de `kmax` de AR-M2V4D que extrapolan fuera del soporte muestreado |
| AOM | La forma de ayuda y de opciones del §5 de `workspace-contract-plan.md`, que decide si `--from` puede aceptar dos clases de valor |
| oqt-V2 | El `GMPETable` que falta en AR-M2V4D —su proyecto no tiene la receta `calcs/gmpe/<modelID>.json`— y el aviso de mudanza cerrada para comprobar los lectores del informe |
| Esta sesión | Retirada de los siete proyectos con `hazard.json` y de los tres sin contrato, por orden del propietario |

Si necesitás algo que no esté acá, preguntame antes de reconstruirlo del código.
