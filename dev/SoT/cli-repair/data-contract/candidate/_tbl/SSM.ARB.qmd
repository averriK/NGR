```{r}
#| include: false
root <- normalizePath(here::here())
SSM_MODEL <- "ARB"
SSM_FILE <- file.path(DataRoot$hazard, "SSMTable.Rds")
if (!exists("CAP", inherits = FALSE)) CAP <- readCaption("tbl.SSM.ARB")
```

&nbsp;

```{r}
#| label: tbl-ssm-arb-inventory
#| tbl-cap: !expr CAP
source(file.path(root, "scripts", "tbl", "SSM.ARB.R"))
TBL
```

&nbsp;

```{r}
#| include: false
rm(list = intersect(c("CAP", "SSM_MODEL", "SSM_FILE"), ls()))
```
